# 🔐 Hash Password with Salt และระบบตรวจสอบการเข้าสู่ระบบ

โปรเจกต์นี้เป็นระบบจัดการรหัสผ่านแบบปลอดภัยที่ใช้เทคนิค **Hash + Salt** สำหรับการเก็บรหัสผ่านผู้ใช้ พร้อมระบบยืนยันตัวตน (Authentication) เบื้องต้น

## 🎯 วัตถุประสงค์

เพื่อให้เข้าใจแนวคิดการจัดเก็บรหัสผ่านอย่างปลอดภัย โดยใช้ Salt + Hash และสามารถนำไปประยุกต์ใช้กับระบบยืนยันตัวตนได้อย่างถูกต้อง

## ✨ คุณสมบัติหลัก

### 1. ระบบสมัครสมาชิก (Register)
- รับ Username และ Password จากผู้ใช้
- สร้าง Salt แบบสุ่มสำหรับแต่ละผู้ใช้
- นำ Password มาต่อกับ Salt แล้วทำการ Hash
- เลือก Hash Algorithm ได้ 5 แบบ:
  - **MD5** - 128-bit hash (ไม่แนะนำ - ใช้เพื่อการศึกษา)
  - **SHA-1** - 160-bit hash (ไม่แนะนำสำหรับ production)
  - **SHA-256** - 256-bit hash (ปลอดภัย ⭐️)
  - **SHA-512** - 512-bit hash (ปลอดภัยมาก ⭐️⭐️)
  - **bcrypt** - Adaptive hash สำหรับ password (ปลอดภัยที่สุด ⭐️⭐️⭐️)
- บันทึกข้อมูล: Username, Salt, Algorithm, Hashed Password
- **ห้ามเก็บรหัสผ่านแบบ Plain Text**

### 2. ระบบเข้าสู่ระบบ (Login)
- รับ Username และ Password
- ดึง Salt และ Algorithm ของผู้ใช้จากระบบ
- Hash Password ที่ป้อนเข้ามาด้วย Salt และ Algorithm เดิม
- เปรียบเทียบกับ Hash ที่บันทึกไว้
- แสดงผลว่า Login สำเร็จหรือไม่

### 3. ฟีเจอร์เพิ่มเติม
- แสดงรายการผู้ใช้ทั้งหมดในระบบ (สำหรับทดสอบ)
- รีเซ็ตระบบ (ลบข้อมูลทั้งหมด)
- UI/UX แบบ Minimal และใช้งานง่าย
- Responsive Design

## 🛠️ เทคโนโลยีที่ใช้

- **React 18** - JavaScript Library สำหรับสร้าง UI
- **TypeScript** - Typed Superset ของ JavaScript
- **Vite** - Build Tool ที่รวดเร็ว
- **Web Crypto API** - API สำหรับ SHA algorithms
- **bcryptjs** - Library สำหรับ bcrypt hashing
- **localStorage** - เก็บข้อมูลผู้ใช้ (client-side)

## 📦 โครงสร้างโปรเจกต์

```text
src/
├── services/
│   ├── hash.ts          # ระบบ Hash Password (MD5, SHA-1, SHA-256, SHA-512, bcrypt)
│   └── store.ts         # ระบบจัดเก็บข้อมูลผู้ใช้ (localStorage)
├── components/
│   ├── Register.tsx     # หน้าสมัครสมาชิก
│   └── Login.tsx        # หน้าเข้าสู่ระบบ
├── App.tsx              # Component หลัก
├── App.css              # Minimal Design System
├── index.tsx            # Entry Point
└── index.css            # Global Styles
```

## 🚀 การติดตั้งและรันโปรเจกต์

### 1. ติดตั้ง Dependencies
\`\`\`bash
npm install
\`\`\`

### 2. รันโปรเจกต์
\`\`\`bash
npm start
\`\`\`

เว็บจะเปิดที่ \`http://localhost:3000\`

### 3. Build สำหรับ Production
\`\`\`bash
npm run build
\`\`\`

## 📚 แนวคิดและหลักการ

### 1. Salt คืออะไร?

**Salt** คือข้อมูลสุ่ม (Random Data) ที่เพิ่มเข้าไปในรหัสผ่านก่อนทำการ Hash

**ตัวอย่าง:**
\`\`\`
Password: "mypassword123"
Salt: "a1b2c3d4e5f6..."
ข้อมูลที่นำไป Hash: "mypassword123a1b2c3d4e5f6..."
\`\`\`

**ประโยชน์ของ Salt:**
- ป้องกัน **Rainbow Table Attack** (ตารางแปลง Hash ที่สร้างไว้ล่วงหน้า)
- ทำให้ผู้ใช้ที่มี Password เดียวกันได้ Hash ที่แตกต่างกัน
- เพิ่มความปลอดภัยให้กับระบบ

**ตัวอย่างความแตกต่าง:**
\`\`\`
User A: Password = "test123", Salt = "abc..." → Hash = "x1y2z3..."
User B: Password = "test123", Salt = "xyz..." → Hash = "p9q8r7..."
\`\`\`
แม้ Password จะเหมือนกัน แต่ Hash ต่างกันเพราะ Salt ต่างกัน!

### 2. ทำไมไม่ควรเก็บรหัสผ่านแบบ Plain Text?

การเก็บรหัสผ่านแบบ Plain Text (ข้อความธรรมดา) มีความเสี่ยงสูงมาก:

❌ **อันตราย:**
- หากฐานข้อมูลถูกเจาะ แฮกเกอร์จะเห็นรหัสผ่านทุกคนทันที
- พนักงานที่มีสิทธิ์เข้าถึงฐานข้อมูลสามารถเห็นรหัสผ่านได้
- ผู้ใช้มักใช้รหัสผ่านเดียวกันหลายเว็บไซต์ ทำให้เสี่ยงต่อการถูกโจมตีในเว็บไซต์อื่น

✅ **วิธีที่ถูกต้อง:**
- เก็บเฉพาะ **Hashed Password** (ไม่สามารถแปลงกลับเป็นรหัสผ่านจริงได้)
- ใช้ **Salt** เพื่อป้องกัน Rainbow Table Attack
- ใช้ **Algorithm ที่ปลอดภัย** เช่น bcrypt, SHA-256, SHA-512

### 3. ความแตกต่างของ Hash Algorithms

| Algorithm | ความยาว Output | ความปลอดภัย | ความเร็ว | แนะนำใช้ |
|-----------|---------------|------------|----------|----------|
| **MD5** | 128-bit | ⚠️ ต่ำ | เร็วมาก | ❌ ไม่แนะนำ |
| **SHA-1** | 160-bit | ⚠️ ปานกลาง | เร็ว | ⚠️ หลีกเลี่ยง |
| **SHA-256** | 256-bit | ✅ สูง | ปานกลาง | ✅ แนะนำ |
| **SHA-512** | 512-bit | ✅ สูงมาก | ปานกลาง | ✅ แนะนำ |
| **bcrypt** | Variable | ✅ สูงสุด | ช้า (ตั้งใจ) | ✅ แนะนำที่สุด |

**คำอธิบาย:**

1. **MD5 และ SHA-1:**
   - มีช่องโหว่ที่รู้จักแล้ว (Collision Attack)
   - ใช้เพื่อการศึกษาเท่านั้น
   - ไม่ควรใช้ในระบบจริง

2. **SHA-256 และ SHA-512:**
   - ปลอดภัยสำหรับการใช้งานทั่วไป
   - เป็นส่วนหนึ่งของ SHA-2 Family
   - SHA-512 ปลอดภัยกว่า SHA-256 แต่ใช้เวลามากกว่าเล็กน้อย

3. **bcrypt:**
   - ออกแบบมาสำหรับ Password Hashing โดยเฉพาะ
   - มีคุณสมบัติ **Adaptive** (ปรับความยากได้)
   - ใช้เวลานานกว่าตั้งใจ เพื่อป้องกัน Brute Force Attack
   - Salt ถูก Generate และเก็บไว้ใน Hash อัตโนมัติ
   - **แนะนำที่สุดสำหรับ Production**

### 4. กระบวนการ Hash + Salt

**ขั้นตอนการสมัครสมาชิก (Register):**
\`\`\`
1. รับ Password จากผู้ใช้: "mypassword123"
2. สร้าง Salt แบบสุ่ม: "a1b2c3d4e5f6..."
3. นำ Password + Salt มารวมกัน: "mypassword123a1b2c3d4e5f6..."
4. Hash ด้วย Algorithm ที่เลือก: "x9y8z7w6v5..."
5. บันทึก: Username, Salt, Algorithm, Hash
\`\`\`

**ขั้นตอนการเข้าสู่ระบบ (Login):**
\`\`\`
1. รับ Username และ Password จากผู้ใช้
2. ดึง Salt และ Algorithm ของผู้ใช้จากฐานข้อมูล
3. นำ Password ที่ป้อน + Salt มารวมกัน
4. Hash ด้วย Algorithm เดิม
5. เปรียบเทียบ Hash ใหม่กับ Hash ที่บันทึกไว้
6. ถ้าตรงกัน → Login สำเร็จ ✅
   ถ้าไม่ตรงกัน → Password ผิด ❌
\`\`\`

## 🔒 หลักการความปลอดภัย

1. **ไม่เก็บ Plain Text Password**
   - เก็บเฉพาะ Hash เท่านั้น

2. **ใช้ Salt ที่ไม่ซ้ำกัน**
   - แต่ละ User ต้องมี Salt เป็นของตัวเอง

3. **เลือก Algorithm ที่ปลอดภัย**
   - แนะนำ: bcrypt > SHA-512 > SHA-256

4. **ไม่เปิดเผยข้อมูลผู้ใช้**
   - ฟีเจอร์แสดงรายการผู้ใช้ใช้เพื่อทดสอบเท่านั้น

## 🎓 การใช้งาน

### สมัครสมาชิก
1. เลือกหน้า "สมัครสมาชิก"
2. กรอก Username (อย่างน้อย 3 ตัวอักษร)
3. กรอก Password และยืนยัน Password (อย่างน้อย 6 ตัวอักษร)
4. เลือก Hash Algorithm
5. กดปุ่ม "สมัครสมาชิก"

### เข้าสู่ระบบ
1. เลือกหน้า "เข้าสู่ระบบ"
2. กรอก Username และ Password
3. กดปุ่ม "เข้าสู่ระบบ"
4. ระบบจะตรวจสอบและแสดงผลว่าสำเร็จหรือไม่

### ทดสอบระบบ
- กดปุ่ม "แสดงข้อมูลผู้ใช้ทั้งหมด" เพื่อดูรายการผู้ใช้
- กดปุ่ม "รีเซ็ตระบบ" เพื่อลบข้อมูลทั้งหมด

## 💡 ความรู้ที่ได้รับ

หลังจากศึกษาโปรเจกต์นี้ คุณจะเข้าใจ:

1. ✅ แนวคิดของ Salt และการใช้งาน
2. ✅ เหตุผลที่ไม่ควรเก็บรหัสผ่านแบบ Plain Text
3. ✅ ความแตกต่างระหว่าง Hash Algorithms ต่างๆ
4. ✅ กระบวนการ Hash + Salt ในระบบจริง
5. ✅ การนำไปประยุกต์ใช้กับระบบ Authentication

## ⚠️ ข้อควรระวัง

- โปรเจกต์นี้ใช้สำหรับการศึกษาเท่านั้น
- ในระบบจริง ควรเก็บข้อมูลฝั่ง Server ไม่ใช่ localStorage
- ควรใช้ HTTPS สำหรับการส่งข้อมูล
- ควรเพิ่ม Rate Limiting เพื่อป้องกัน Brute Force Attack

## 👨‍💻 ผู้พัฒนา

โปรเจกต์นี้พัฒนาด้วย React + TypeScript โดยใช้หลักการ Hash Password with Salt ตามมาตรฐานความปลอดภัย

## 📄 License

MIT License - สามารถนำไปใช้เพื่อการศึกษาได้อย่างอิสระ
